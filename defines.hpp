/* The #define and const directives, global variables */

#ifndef DEFINES_HPP
#define DEFINES_HPP

#include <SFML/Graphics.hpp>

const int ITEM_DIMENSIONS = 64;

// sizes of window
extern int WINDOW_W;
extern int WINDOW_H;

extern sf::Font GLOBAL_FONT;
extern sf::FloatRect BORDERS;

#endif
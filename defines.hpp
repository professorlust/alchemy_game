/* Constant values, global #include's and defines */
#ifndef DEFINES_HPP
#define DEFINES_HPP

#include <SFML\Graphics.hpp>

//sizes of the main window
const int WINDOW_W = 800;
const int WINDOW_H = 600;

const int ELEMENT_DIMENSIONS = 64; // length of the side of the square of the element with the picture

#endif